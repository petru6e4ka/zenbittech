import React from "react";
import Common from "src/layouts/Common";
import { FeedbackForm } from "src/modules";

export const FeedBack = () => {
  return (
    <Common>
      <FeedbackForm />
    </Common>
  );
};

export default FeedBack;
